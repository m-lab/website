# Sets the baseurl to an empty string to be used before deploying to s3
# command to implement only for the _config.yml file is:
# sed -f _scripts/baseurl.sed _config.yml > _scripts/_config.yml

s/\/m-lab\.github\.io//g;